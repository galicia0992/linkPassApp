export declare function WavePath(maxPoints: number, speed: number, delta: number): {
    getPoints: (factor: number, width: number, height: number) => {
        build: (WIDTH: number, HEIGHT: number) => string;
    };
};
